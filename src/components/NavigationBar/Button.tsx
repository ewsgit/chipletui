import React from "react"
import {IconTypings} from "../../iconTypings"
import "./../defaults.css"

export default function Button(props: {name: string, icon: IconTypings}) {
    return <div></div>
}
